Add your licensed Canela font files here with these exact names
(the site's CSS already points to them):

  Canela-Regular.woff2       (weight 400, normal)
  Canela-Regular.woff        (weight 400, normal - fallback for older browsers)
  Canela-Medium.woff2        (weight 500, normal)
  Canela-Medium.woff         (weight 500, normal - fallback)
  Canela-RegularItalic.woff2 (weight 400, italic)
  Canela-RegularItalic.woff  (weight 400, italic - fallback)

Until these files are added, headings will automatically display in
Cormorant Garamond (a free, visually similar serif) so the site still
looks correct in the meantime.
